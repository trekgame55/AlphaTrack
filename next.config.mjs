/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: ['194.37.80.126'],
  devIndicators: {
    appIsrStatus: false,
    buildActivity: false,
  },
};

export default nextConfig;
